# Required only when running the Discord interface.
DISCORD_BOT_TOKEN=

# Optional: sync slash commands to one development server immediately.
# Leave empty to sync globally (global command propagation can take time).
DISCORD_GUILD_ID=

# Optional Bybit public-market setting. No Bybit API key or secret is needed.
BYBIT_TESTNET=false
